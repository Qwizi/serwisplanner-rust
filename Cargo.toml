[package]
name = "serwis-planner"
version = "2026.325.2"
edition = "2021"
description = "Rust client library for the Serwis Planner API"
license = "MIT"
repository = "https://github.com/Qwizi/serwisplanner-rust"

[dependencies]
reqwest = { version = "0.12", default-features = false, features = ["json", "multipart", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
tokio = { version = "1", features = ["full"] }

[dev-dependencies]
tokio = { version = "1", features = ["full", "test-util"] }
